
% The following list of packages used by my dissertation.

\usepackage[dvipdfm]{graphicx}
\usepackage{amsbsy}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amstext}
\usepackage{boxedminipage}
\usepackage[dvipdfm]{color}
\usepackage{comment}
\usepackage{fancyvrb}
\usepackage[english]{fp-autoref}
\usepackage{fp-frame}
\usepackage{ifthen}
\usepackage{listings}
\usepackage{mathpartir}
\usepackage{stmaryrd}
\usepackage{times}
\usepackage[hyphens]{url}

% Package hyperref wants to be the last package loaded.
\usepackage[dvips]{hyperref}  % See NOTES.txt for more information.

% =====================
% Package configuration
% =====================

% Define Scalaness language support.
% The keywords listed here include all reserved words from Scala along with nesT/Scalaness
% extensions.
\lstdefinelanguage{scalaness}{keywords=
  {abbrvt, abstract, case, catch, class, def, do, else, extends, false, final, finally, for,
   forSome, if, implicit, import, lazy, match, new, null, object, override, package, private,
   protected, return, sealed, super, this, throw, trait, try, true, type, val, var, while, with,
   yield},
  otherkeywords={export, typedef, _, :, =, =>, <-, <:, <\%, >:, \#, @},
  basicstyle=\ttfamily,
  sensitive=true,
  morecomment=[l]//,
  morecomment=[s]{/*}{*/},
  morestring=[b]{"}}

% Define nesC language support.
% The keywords listed here include all the "normal" (non-underscore leading) keywords from C2011
% together with additional keywords from nesC and SpartanRPC.
\lstdefinelanguage{nesC}{keywords=
  {activate, as, auto, break, case, char, const, continue, default, do, double, duty, else,
   enable, enum, extern, float, for, goto, if, inline, int, long, register, restrict, return,
   short, signed, sizeof, struct, switch, typedef, union, unsigned, void, volatile, while, as,
   atomic, async, call, command, component, components, configuration, event, generic,
   implementation, includes, interface, module, new, norace, nx_struct, nx_union, post,
   provides, remote, requires, signal, task, uses},
  otherkeywords={import, export, ->},
  basicstyle=\ttfamily,
  sensitive=true,
  morecomment=[l]//,
  morecomment=[s]{/*}{*/},
  morestring=[b]{"}}

% Default settings for code listings.

\lstset{language=scalaness,
  basicstyle={\small},  % If you turn off \ttfamily you get bold keywords.
  stringstyle=\ttfamily,
  commentstyle=\ttfamily,
  xleftmargin=0.25in,
  showstringspaces=false}

\lstset{language=nesC,
  basicstyle={\small},  % If you turn off \ttfamily you get bold keywords.
  stringstyle=\ttfamily,
  commentstyle=\ttfamily,
  xleftmargin=0.25in,
  showstringspaces=false}

% This allows links to wrap and it makes the hyperlinks in the document invisible.
% See NOTES.txt for more information.
\hypersetup{
  breaklinks=true,
  colorlinks=false,
  pdfborder={0 0 0},
}
